# Server only. Copy this file to .env and add your Vercel AI Gateway key.
AI_GATEWAY_API_KEY=
PORT=3000
# Optional: model that builds the Coding Navigator's solution maps.
# Must support JSON-schema output through the Gateway.
# TUTOR_MAP_MODEL=google/gemini-2.5-flash-lite
